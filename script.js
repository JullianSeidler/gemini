// Adiciona a classe 'loaded' ao body quando o conteúdo da página estiver carregado,
// permitindo a animação de fade-in definida no CSS.
document.addEventListener('DOMContentLoaded', () => {
    document.body.classList.add('loaded');
});
